{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block content %}

<style>
	.avi{
		display: flex;
		justify-content:center;
	}
	.avatar{
		border-radius:25%;

	
		
		width:70px;
		
	}
</style>

	<h1 class=" text-center " style="margin-top: 50px;">Mon compte</h1>
	<h6 class=" text-center ">{{app.user.prenom}}</h6>
	<div class=" col-md-12 avi"><img class="avatar "src="{{ asset('uploads/photo/' ~ app.user.photo) }}"></div>

  
    <a href="{{path('compte_edit')}}">Modifier mes informations</a>
            {% if is_granted('ROLE_ADMIN') %} 
						<li class="nav-item dropdown" style="list-style:none">
							<a class="nav-link dropdown-toggle" href="{{ path('admin') }}" id="dropdown04" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Adminitration</a>
							<div class="dropdown-menu" aria-labelledby="dropdown04">
								<a class="dropdown-item" href="{{ path('admin') }}">Accueil </a>
								<a class="dropdown-item" href="{{ path('admin_membres') }}">Gestion des membres</a>
								<a class="dropdown-item" href="{{ path('admin_fighters') }}">Gestion des combatants</a>
								{# <a class="dropdown-item" href="{{ path('') }}">Gestion du forum</a> #}
                            </div>
						</li>

												
						 <small class="btn btn-primary"><a class="dropdown-item" href="{{ path('forum_forum') }}">Accès Forum</a></small> 
             
			{% endif %} 

{% endblock %}
